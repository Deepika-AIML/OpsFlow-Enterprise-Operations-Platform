# Core framework
Django>=5.0,<5.2
djangorestframework>=3.15,<3.16

# Authentication
djangorestframework-simplejwt>=5.3.1
argon2-cffi>=23.1.0

# Database (MySQL)
mysqlclient>=2.2.4

# API niceties
django-cors-headers>=4.3.0
django-filter>=24.2
drf-spectacular>=0.27.0

# Config / env
django-environ>=0.11.2

# Static files (used only by the production settings profile)
whitenoise>=6.6.0

# Production WSGI server (used only by the production settings profile)
gunicorn>=22.0.0

# Testing
pytest>=8.2.0
pytest-django>=4.8.0
flake8>=7.0.0
